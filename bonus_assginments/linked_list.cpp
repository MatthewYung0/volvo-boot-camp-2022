#include <iostream>

    struct Node {
        int data;
        Node* link;
    };

int main() {

    // //Declaring 4x node's
    // Node head;
    // Node body_one;
    // Node body_two;
    // Node body_three;

    // //Assigning values to data each node.
    // head.data = 5;
    // body_one.data = 10;
    // body_two.data = 20;
    // body_three.data = 25;

    // //Assigning the size of pointers in each node.
    // struct Node* head = (struct Node*)malloc(sizeof(struct Node));
    // struct Node* body_one = (struct Node*)malloc(sizeof(struct Node));
    // struct Node* body_two = (struct Node*)malloc(sizeof(struct Node));
    // struct Node* body_three = (struct Node*)malloc(sizeof(struct Node));

    Node *head_ptr;
    

}